CREATE DATABASE tyrell_corp;USE tyrell_corp;
CREATE TABLE nexus6 (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL
);
INSERT INTO nexus6 (name) VALUES ('Leon');
GRANT SELECT ON tyrell_corp.nexus6 TO 'holberton_user'@'localhost';
FLUSH PRIVILEGES;

Note that all this query are to be executed in our web-01 and in order to switch to this use either <br>
1. ssh ubuntu@<ip-address> or
2. ssh -i ~/.ssh/school ubuntu@<ip-address> where the ~/.ssh/school is the path to your private key
